const car = {
    color: "white",
    seats: 2,
    nitros: false
}

// const msg = "My car have " + car.seats + " seats and it is of color " + car.color
const msg = `My car have ${car.seats} seat and it is of color ${car.color}`

console.log(msg)